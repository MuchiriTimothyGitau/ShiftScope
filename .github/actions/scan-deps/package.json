{
  "name": "shiftscope-scan-action",
  "version": "1.0.0",
  "private": true,
  "description": "ShiftScope dependency scan GitHub Action",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx -y esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js --external:@actions/core --external:@actions/github",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0"
  }
}
